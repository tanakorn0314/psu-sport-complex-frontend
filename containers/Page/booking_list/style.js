import styled from 'styled-components';

const StyledWapper = styled.div`
    text-align: center
    .back {
        text-align: left
    }
    .list-item {
        cursor: pointer;
    }
`;

export default StyledWapper;