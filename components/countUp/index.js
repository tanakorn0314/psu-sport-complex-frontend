import styled from 'styled-components';
import CountUp from 'react-countup';
import fonts from '../../styles/fonts';
import colors from '../../styles/colors';

export default styled(CountUp)`
    font-family: ${fonts.psuStidti};
    color: ${colors.main1};
    font-weight: ligher;
    font-size: 32px;
`