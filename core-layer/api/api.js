const local = 'http://localhost:3001/api'

const api = local;

export const userApi = `${api}/user`
export const authApi = `${api}/auth`
export const bookingApi = `${api}/booking`
export const stadiumApi = `${api}/stadium`